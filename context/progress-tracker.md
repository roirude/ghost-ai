# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Project Dialogs & Editor Home (`context/feature-specs/04-project-dialogs.md`) — complete.

## Current Goal

- Define the next feature unit from `context/feature-specs/`.

## Completed

- Design system and UI primitives (`context/feature-specs/01-design-system.md`): shadcn/ui configured via hand-authored `components.json` and `components/ui/*` (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea) using classic Radix-primitive conventions; `lucide-react` installed; `lib/utils.ts` created with the standard `clsx` + `tailwind-merge` `cn()` helper; dark theme tokens from `context/ui-context.md` added to `app/globals.css` and mapped to shadcn's expected CSS variables. Verified: `tsc --noEmit`, `next build`, `eslint` all pass; a temporary scratch route rendered all 7 components and a Dialog in a headless browser with zero console errors and correct dark styling (no light-mode artifacts), then was removed.
- Editor base shell (`context/feature-specs/02-editor.md`): `components/editor/editor-navbar.tsx` (fixed-height top bar, left/center/right sections, sidebar toggle button swapping `PanelLeftOpen`/`PanelLeftClose` via an `isSidebarOpen` prop, dark surface background with bottom border); `components/editor/project-sidebar.tsx` (absolutely positioned floating overlay that slides in/out from the left via a `translate-x` transition driven by `isOpen` so it never pushes canvas content, header with title + close button, shadcn `Tabs` for "My Projects" / "Shared" each with an empty placeholder state, full-width `New Project` button with a `Plus` icon pinned to the bottom); `components/editor/app-dialog.tsx` (a reusable composition wrapper around `components/ui/dialog.tsx` accepting `title`, optional `description`, optional `footer`, and `children`, so future features compose dialogs without touching the protected `components/ui/dialog.tsx`) — no actual dialog instances were wired up, per spec. Verified: `next build` and `eslint` both pass; a temporary scratch route (`app/ui-check`) rendered the navbar, sidebar (open/closed), and an `AppDialog` instance in a headless browser via `npx playwright screenshot`, confirming dark theming, no light-mode artifacts, and correct slide/toggle behavior, then was removed.
- Project Dialogs & Editor Home (`context/feature-specs/04-project-dialogs.md`): `types/project.ts` defines a mock `Project` interface (`id`, `name`, `slug`, `isOwner`); `lib/mock-projects.ts` holds `MOCK_OWNED_PROJECTS`/`MOCK_SHARED_PROJECTS` sample data and a small `slugify()` helper (no persistence). `hooks/use-project-dialogs.ts` is a client hook owning dialog state (`{type: "create"} | {type: "rename", project} | {type: "delete", project} | null`), the shared name/slug form state, and a mock `isLoading` flag with a simulated async `submit()`. Three new dialog components under `components/editor/` (`create-project-dialog.tsx`, `rename-project-dialog.tsx`, `delete-project-dialog.tsx`) all compose the existing `AppDialog` — create shows a live slug preview under the input, rename prefills the name and auto-focuses with Enter-to-submit, delete has no input and uses the `destructive` `Button` variant. `components/editor/editor-home.tsx` is the new `/editor` empty-state (heading, description, `New Project` button with a `Plus` icon, no cards). `components/editor/project-sidebar.tsx` now renders the mock project lists per tab with a `ProjectListItem` row that reveals rename/delete icon buttons on hover only when `project.isOwner` is true (Shared tab items render with no actions), plus a mobile-only (`md:hidden`) backdrop scrim rendered when `isOpen` that calls `onClose` on click/tap. `app/editor/editor-shell.tsx` wires everything: renders `EditorHome` beside the sidebar, mounts all three dialogs keyed off the hook's `dialog` state, and passes `openCreateDialog`/`openRenameDialog`/`openDeleteDialog` down to both the sidebar and the editor home's `New Project` button. Verified: `tsc --noEmit` and `eslint .` both pass clean across the whole project; `next build` succeeds. Visual verification used a temporary public scratch route (`app/ui-check-dialogs`) plus a temporary `"/ui-check-dialogs(.*)"` entry in `proxy.ts`'s public-route matcher (since `/editor` is auth-protected) and a temporarily `npm install --no-save`'d `playwright` to script interactions — `npx playwright` screenshots at 1440x900 and 390x844 confirmed: the editor home empty state, sidebar open/closed slide, hover-revealed rename/delete icons on owned projects only (absent on the Shared tab), all three dialogs' content and styling (including the destructive red delete button and live `ghost.ai/<slug>` preview updating as typed), and the mobile backdrop scrim appearing on open and closing the sidebar on an outside tap. The scratch route, the `proxy.ts` matcher entry, and the temporary `playwright` install were all removed afterward — `git status` confirms no leftover diff in `proxy.ts` or `package.json`/`package-lock.json`.
- Authentication (`context/feature-specs/03-auth.md`): `@clerk/ui@1.32.3` installed. `proxy.ts` added at the project root (Next.js 16 renamed `middleware.ts` to `proxy.ts` — confirmed via `node_modules/next/dist/docs`) using `clerkMiddleware` + `createRouteMatcher` from `@clerk/nextjs/server`; public routes are derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL`, everything else calls `auth.protect()`. Those two env vars did not exist yet (only the publishable/secret keys did) — added `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` to `.env.local` since they are Clerk's own conventional variable names (confirmed by reading `@clerk/nextjs`'s `constants.js`) and the spec requires driving routing off them rather than hardcoding paths. `app/layout.tsx` now wraps `{children}` in `ClerkProvider` using `@clerk/ui`'s `dark` theme from `@clerk/ui/themes`, with `appearance.variables` mapped to the app's existing CSS custom properties (`var(--bg-surface)`, `var(--accent-primary)`, etc.) instead of hardcoded colors. `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` render Clerk's `<SignIn />`/`<SignUp />` inside a shared `components/auth/auth-layout.tsx` (two-panel on large screens — left brand/tagline/text feature list, right centered form; form-only below `lg`; no gradients, hero sections, feature cards, or scroll). `app/page.tsx` is now a server component that calls `auth()` and redirects to `/editor` (signed in) or `/sign-in` (signed out). `components/editor/editor-navbar.tsx` (protected foundation-adjacent but project-owned, not `components/ui/*`) got Clerk's `<UserButton />` added to its right section. Added `app/editor/page.tsx` + `app/editor/editor-shell.tsx` (a small client component owning `isSidebarOpen` state) since no route rendered `EditorNavbar`/`ProjectSidebar` yet and the spec's done-check requires `UserButton` to be reachable in the actual editor navbar. Verified: `tsc --noEmit`, `eslint`, and `next build` all pass (build output confirms `ƒ Proxy (Middleware)`); against the project's own already-running dev server, `curl` confirmed `/` → 307 to `/sign-in`, `/editor` → 307 (protected), `/sign-in` → 200; `npx playwright screenshot` at 1440x900 and 390x844 confirmed the two-panel/form-only responsive behavior and correct dark/cyan-accent theming with no light-mode artifacts on both `/sign-in` and `/sign-up`.

## In Progress

- None currently.

## Next Up

- Add the next planned feature unit here (only `01-design-system.md`, `02-editor.md`, `03-auth.md`, and `04-project-dialogs.md` exist in `context/feature-specs/` so far).

## Open Questions

- None currently.

## Architecture Decisions

- `app/globals.css` previously had no theme at all (`@import "tailwindcss";` only). The dark theme token table in `context/ui-context.md` was not yet implemented in CSS, so it was added here as part of this unit rather than deferred, since shadcn components need real tokens to render dark-first and the spec's own done-check ("No default light styling appears") depends on it.
- The installed `shadcn` CLI (v4.21.0) has moved to a new default architecture (`base-nova`/`radix-nova` presets, `@base-ui/react` or a bundled `cn` npm package, `@tabler/icons-react` as default icon lib) that no longer matches classic shadcn/ui conventions or this project's spec (which calls for a locally-defined `cn()` and `lucide-react`). Rather than accept CLI defaults, `components.json` and all `components/ui/*` files were hand-authored using classic Radix-primitive shadcn/ui source, restyled to this project's token names. Future `shadcn add` CLI usage should be checked against this convention before trusting its output.
- Next.js 16 deprecated and renamed the `middleware.ts` file convention to `proxy.ts` (function renamed `middleware` → `proxy`); `context/feature-specs/03-auth.md` calling for `proxy.ts` over `middleware.ts` is this rename, not a project-specific choice — confirmed against `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`.
- `@clerk/nextjs@7.9.2`'s `createRouteMatcher` is marked `@deprecated` in favor of resource-based auth checks (auth calls inside each page/route instead of centralized path matching), per its own type doc comment. Used it anyway here because the spec explicitly asks for public-route matching driven by env vars in `proxy.ts`, which is inherently a route-matcher pattern — this is a deliberate spec-driven exception, not an oversight. If Clerk removes `createRouteMatcher` in a future major version, this will need to move to per-route `auth.protect()` calls.
- `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` did not exist in `.env.local` before this unit (only the publishable/secret keys did). Added them rather than hardcoding `/sign-in` and `/sign-up` path strings in `proxy.ts`, since the spec says to define public routes "using the existing sign-in and sign-up env vars" and these are Clerk's own standard variable names (read directly by `@clerk/nextjs`'s internals) rather than app-invented ones.

## Session Notes

- `components/ui/*` are the protected foundation files per `context/ai-workflow-rules.md` — do not hand-edit them again except through deliberate re-generation; app-level styling/composition belongs in feature components instead.
- No route previously rendered `EditorNavbar` / `ProjectSidebar`; `app/editor/page.tsx` + `app/editor/editor-shell.tsx` were added in this unit as the first real usage, since the auth spec's done-check depends on `UserButton` being reachable in the actual editor navbar, not just the component file.
